#summary How to run the App in your own machine
Before being able to run the app successfully, you must do the following first:
  * Register the app URL with OAuth Providers (Facebook, Google, Twitter etc.). It is necessary because OAuth protocol works by Web redirection.